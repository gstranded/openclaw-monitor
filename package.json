{
  "name": "openclaw-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal backend aggregation API for OpenClaw monitor dashboard",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/data.js && node --check src/router.js",
    "test": "NODE_ENV=test node --test test/*.test.js"
  }
}
