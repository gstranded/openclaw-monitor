# Vite exposes only variables prefixed with VITE_
# If backend is mounted on the same origin, keep default.
VITE_API_BASE=/api
# Auto refresh interval in ms (default 10000)
VITE_DASHBOARD_POLL_MS=10000
